@model IEnumerable<lab6.Models.ViewModel>

@{
	ViewData["Title"] = "Index";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
	<a asp-action="Create">Create New</a>
</p>
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.FirstOrDefault().ErdmiinZereg.Name)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.FirstOrDefault().Info.CreatedBy)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.ErdmiinZereg.Name)
				</td>


				<td>
					@Html.DisplayFor(modelItem => item.ErdmiinZereg.Info.CreatedBy)
				</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.ErdmiinZereg.Id">Edit</a> |
					<a asp-action="Details" asp-route-id="@item.ErdmiinZereg.Id">Details</a> |
					<a asp-action="Delete" asp-route-id="@item.ErdmiinZereg.Id">Delete</a>
				</td>
			</tr>
		}
	</tbody>
</table>
